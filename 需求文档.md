# 主机订阅管理系统需求文档

## 1. 项目概述
主机订阅管理系统是一款桌面应用程序，主要用于管理本地hosts文件和监控网络IP地址变更，并支持通过邮件自动发送IP地址变更通知。该系统支持Windows、Unix/Linux和Mac操作系统，提供了直观的图形用户界面和系统托盘功能。

## 2. 功能需求

### 2.1 Hosts文件管理功能

#### 2.1.1 查看Hosts文件内容
- **功能描述**：允许用户查看当前系统的hosts文件内容
- **输入**：用户点击"查看hosts文件"按钮
- **输出**：在控制台区域显示hosts文件内容（过滤掉注释行）

#### 2.1.2 添加/更新Hosts条目
- **功能描述**：允许用户添加或更新主机名与IP地址的映射关系
- **输入**：
  - IP地址（支持IPv4和IPv6）
  - 主机名
  - 可选的别名
- **验证规则**：
  - IP地址格式验证（IPv4和IPv6）
  - 主机名格式验证（长度不超过253字符，只能包含字母、数字、连字符和点，不能以连字符或点开头或结尾）
- **处理流程**：
  1. 检查用户是否具有管理员权限
  2. 如有管理员权限，直接更新hosts文件
  3. 如无管理员权限，请求管理员权限后更新
  4. 备份原始hosts文件
  5. 更新hosts文件内容

#### 2.1.3 删除Hosts条目
- **功能描述**：允许用户删除指定主机名的映射条目
- **输入**：主机名
- **处理流程**：
  1. 检查用户是否具有管理员权限
  2. 如有管理员权限，直接删除hosts文件中的对应条目
  3. 如无管理员权限，请求管理员权限后删除
  4. 备份原始hosts文件
  5. 更新hosts文件内容

### 2.2 网络IP地址监控功能

#### 2.2.1 显示本机IP信息
- **功能描述**：显示本机所有网络接口的IP地址信息
- **输出信息**：
  - 网卡名称
  - IP地址类型（IPv4或IPv6）
  - 地址类型（稳定地址或临时地址，仅IPv6）
  - IP地址
- **过滤规则**：
  - 排除回环地址
  - 排除本地链接地址
  - 排除包含"0:0:0"的地址
  - 排除IPv6本地链接地址（以fe80开头）

### 2.3 IP地址变更通知功能

#### 2.3.1 邮箱配置
- **功能描述**：允许用户配置用于发送IP地址变更通知的邮箱参数
- **配置项**：
  - 设备名称（自动使用本机主机名，可修改）
  - 邮箱地址
  - SMTP服务器
  - 端口（默认465）
  - 授权码
  - 自动发送开关
- **验证规则**：
  - 设备名称格式验证（符合域名格式）
  - 邮箱地址格式验证
  - 端口号范围验证（1-65535）

#### 2.3.2 测试邮件发送
- **功能描述**：允许用户测试邮箱配置是否正确
- **处理流程**：
  1. 验证邮箱配置参数
  2. 在后台线程中发送测试邮件
  3. 显示测试结果（成功或失败）

#### 2.3.3 自动IP地址变更检测与通知
- **功能描述**：定时检测本机IP地址变更，并在发生变更时自动发送邮件通知
- **检测频率**：每10秒检测一次
- **检测逻辑**：
  1. 获取当前本机IP地址列表
  2. 与上次保存的IP地址列表进行比较
  3. 如发现新增或变更的IP地址，且自动发送开关开启，则发送邮件通知
  4. 更新本地保存的IP地址列表

### 2.4 系统托盘功能

#### 2.4.1 最小化到系统托盘
- **功能描述**：当用户最小化应用程序窗口时，应用程序会隐藏窗口并显示在系统托盘中

#### 2.4.2 系统托盘菜单
- **功能描述**：提供系统托盘右键菜单
- **菜单项**：
  - Open：打开应用程序窗口
  - Exit：退出应用程序

#### 2.4.3 系统托盘图标交互
- **功能描述**：支持通过点击系统托盘图标操作应用程序
- **交互方式**：
  - 单击：显示应用程序窗口
  - 双击：恢复应用程序窗口

## 3. 非功能需求

### 3.1 性能需求
- IP地址检测响应时间：< 1秒
- 邮件发送响应时间：< 5秒
- 应用程序启动时间：< 3秒

### 3.2 兼容性需求
- 支持Windows操作系统
- 支持Unix/Linux操作系统
- 支持Mac操作系统

### 3.3 安全性需求
- 管理员权限请求：在需要修改系统文件时，请求用户授予管理员权限
- 邮箱授权码处理：安全存储和传输邮箱授权码

### 3.4 可用性需求
- 直观的图形用户界面
- 清晰的错误提示信息
- 系统托盘功能，方便用户随时访问
- 自动保存配置信息

## 4. 系统架构

### 4.1 核心模块

| 模块名称 | 主要功能 | 核心类 |
|---------|---------|-------|
| 主应用模块 | 应用程序入口，UI管理 | HostsSubscribeApplication.java |
| Hosts文件服务 | Hosts文件的读取、写入、更新和删除 | HostsFileService.java |
| 网络服务 | 网络IP地址获取、监控和比较 | NetworkService.java |
| 邮件服务 | 邮箱配置管理和邮件发送 | EmailService.java, EmailSender.java |
| 邮箱配置界面 | 邮箱参数配置UI | EmailConfigView.java |
| 应用配置 | 应用程序配置管理 | AppConfiguration.java |

### 4.2 数据模型

- **HostEntry**：主机条目模型
- **IPInfoDTO**：IP地址信息模型
- **IPv6Address**：IPv6地址模型
- **EmailConfig**：邮箱配置模型

## 5. 技术实现

### 5.1 开发语言
- Java

### 5.2 主要框架和库
- Java Swing：图形用户界面
- Hutool：工具库
- Guava：工具库

### 5.3 关键技术点
- 系统托盘实现
- 管理员权限获取
- IPv4/IPv6地址验证
- 定时任务调度
- 邮件发送功能

## 6. 用户界面设计

### 6.1 主界面布局
- **工具面板**：包含"邮箱订阅设置"、"显示本机IP信息"、"查看hosts文件"按钮
- **配置hosts面板**：包含host输入框和保存/删除按钮
- **控制台面板**：显示操作结果和信息

### 6.2 邮箱配置对话框
- 设备名称输入框
- 邮箱地址输入框
- SMTP服务器输入框
- 端口输入框
- 授权码输入框
- 自动发送复选框
- 测试发送、确定、取消按钮

## 7. 部署说明

### 7.1 Windows系统
- 可执行jar文件
- 支持通过装配描述符生成Windows可执行文件

### 7.2 Unix/Linux系统
- 可执行jar文件
- 提供部署脚本

### 7.3 Mac系统
- 可执行jar文件

## 8. 维护与支持

### 8.1 日志记录
- 使用SLF4J和Logback进行日志记录
- 记录应用程序启动、操作和错误信息

### 8.2 错误处理
- 提供清晰的错误提示信息
- 记录详细的错误日志
- 异常情况下保证数据安全

## 9. 扩展功能规划

- 支持批量导入/导出hosts文件
- 支持多个邮箱接收通知
- 支持定时备份hosts文件
- 支持多语言界面
- 支持更多类型的IP地址变更通知方式（如微信、短信等）